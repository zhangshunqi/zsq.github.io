#include "parser.h"
#include <stdio.h>

int main() {
    parse("Hello Git & GCC!");
    // 分支 feature/linecount 中添加行统计测试
    // int lines = count_lines("main.c");
    // printf("Line count of main.c: %d\n", lines);
    return 0;
}
